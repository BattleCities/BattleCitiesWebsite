# Battle Cities Website

Static landing page for Battle Cities, ready for GitHub Pages.

## Publish

1. In the repository's **Settings → Pages**, select **GitHub Actions** as the source.
2. Push the `main` branch. The included workflow deploys the site automatically.

The landing page links its primary action to `https://battlecities.com`. Update that URL in `index.html` if the game moves.
